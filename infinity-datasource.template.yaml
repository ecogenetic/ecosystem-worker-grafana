apiVersion: 1

datasources:
  - name: ecosystem-infinity-datasource
    type: yesoreyeram-infinity-datasource
    access: proxy
    url: "http://ecosystem-server:3001/api" 
    jsonData:
      auth_method: "bearerToken"
    secureJsonData:
      bearerToken: "${JWT_TOKEN}"
